# JWKS の鍵の種類を確認
  cat .next/standalone/data/cf-jwks.json | node -e "
  const j=require('fs').readFileSync('/dev/stdin','utf-8');
  const d=JSON.parse(j);
  d.keys.forEach((k,i)=>console.log('Key',i,': kty='+k.kty, 'alg='+(k.alg||'none'), 'kid='+k.kid));
  "
